services:
  paperclip:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${CYBERPUNK_PORT:-3100}:3100"
    environment:
      HOST: "0.0.0.0"
      CYBERPUNK_HOME: "/paperclip"
      OPENAI_API_KEY: "${OPENAI_API_KEY:-}"
      ANTHROPIC_API_KEY: "${ANTHROPIC_API_KEY:-}"
      CYBERPUNK_DEPLOYMENT_MODE: "authenticated"
      CYBERPUNK_DEPLOYMENT_EXPOSURE: "private"
      CYBERPUNK_PUBLIC_URL: "${CYBERPUNK_PUBLIC_URL:-http://localhost:3100}"
      BETTER_AUTH_SECRET: "${BETTER_AUTH_SECRET:?BETTER_AUTH_SECRET must be set}"
    volumes:
      - "${CYBERPUNK_DATA_DIR:-./data/docker-paperclip}:/paperclip"
